import {StatusValueField} from "../../components/particle/Field";
import {fieldType} from "../../type/type";

describe("StatusValueField", () => {
    test("表示がされる", () => {
        const defaultValue = 0
        const max = 252
        const label = "努力値"
        const onChange = (input: fieldType) => {

        }
    })
})